import { makeStyles } from "@material-ui/core/styles";

export const signInViewStyles= makeStyles((theme) => ({
      root:{
        padding: "60px 0px",
      },
      loginForm:{
          margin:`0px auto`,
          maxWidth: '320px'
      }
  })); 
